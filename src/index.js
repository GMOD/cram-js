import {
  parseStream,
  parseFile,
  parseStringSync,
  formatSync,
  formatStream,
  formatFile,
} from './api'

import * as util from './util'

export default {
  parseStream,
  parseFile,
  parseStringSync,
  formatSync,
  formatStream,
  formatFile,
  util,
}
